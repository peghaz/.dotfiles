# ~/.config/fish/config.fish

# Keep this file minimal.
# Most OMF-specific config lives in ~/.config/omf/init.fish.

if status is-interactive
    # Disable the default Fish greeting.
    set -g fish_greeting
end
